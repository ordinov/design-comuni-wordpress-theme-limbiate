<?php

function limbiate_events_calendar_shortcode($atts)
{
    // $atts = shortcode_atts(['posts' => 4], $atts, 'latest_posts');

    $output = '';

    return $output;
}

add_shortcode('events_calendar', 'limbiate_events_calendar_shortcode');
